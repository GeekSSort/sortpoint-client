/**
 * The same Sales screen the back office shows, rendered inside the POS shell.
 * Re-exported rather than copied so the two can never drift apart.
 */
export { default } from "@/app/(dashboard)/sales-pos/sales/page";
